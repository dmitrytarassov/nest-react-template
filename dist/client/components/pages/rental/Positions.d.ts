/// <reference types="react" />
interface UniquesProps {
    loadPositionUrl: string;
    linkToAll: string;
    revertColors: boolean;
}
declare const Positions: ({ loadPositionUrl, linkToAll, revertColors, }: UniquesProps) => JSX.Element;
export default Positions;
