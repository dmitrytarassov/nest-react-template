import React from 'react';
import { City } from '@lib/types/City';
export declare const CityProvider: ({ children, currentCity, }: {
    children: React.ReactNode;
    currentCity: City;
}) => JSX.Element;
